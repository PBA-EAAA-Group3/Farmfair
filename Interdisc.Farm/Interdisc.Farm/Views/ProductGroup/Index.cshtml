@model IEnumerable<Interdisc.Farm.Models.ProductGroupModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductGroupName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductGroupName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProductGroupModelId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProductGroupModelId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProductGroupModelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
